# Excel & Power BI Business Analytics T1 (Холдинг)

## Описание проекта

Провести комплексный анализ данных с использованием Excel, VBA и Power BI с целью обработки информации, расчёта бизнес-метрик, автоматизации аналитических процессов и визуализации ключевых показателей для принятия решений.

В рамках проекта выполнены следующие задачи:

- обработка и структурирование данных в Excel  
- расчёт ключевых бизнес-метрик (выручка, ROI, показатели эффективности)  
- работа со сводными таблицами и аналитическими отчётами  
- автоматизация расчётов и процессов с помощью VBA (макросы)  
- подготовка данных для дальнейшего анализа  
- построение дашбордов и визуализаций в Power BI  
- анализ данных и формирование выводов  

## 📂 Исходные данные

В работе используются данные по заявкам, содержащие:

- `Внешний номер` — идентификатор заявки  
- `Банкомат` — ID банкомата  
- `Регион` — регион  
- `РОО` — подразделение  
- `Получатель` — получатель заявки  
- `Тип сбоя` — тип проблемы  
- `Дата отправки` — дата начала работ  
- `Дата окончания работ` — дата завершения работ  
- `В расчете доступности` — учитывается ли заявка в доступности  
- `Просрочено` — признак просрочки  
- `Повторно` — повторная заявка  
- `Платно` — платная заявка  
- `Отменено` — статус отмены  
---

## 🛠  Построение дашборда (Power BI)

В рамках задания необходимо:

- построить удобный, читаемый и информативный дашборд  
- визуализировать ключевые показатели для анализа  
- обеспечить понятную структуру для пользователя  

Дополнительно учтено:
- возможность масштабирования данных  
- корректная работа при увеличении объёма (накопление истории)  
- универсальность модели данных  

---

## 📊 Ключевые метрики

- количество заявок  
- доля повторных заявок  
- доля просроченных заявок  
- средняя длительность выполнения  

---

## 📈 Визуализации

### 📌 Количество заявок
![Результат](здесь будет рисунок)

---

### 📌 Доля повторных заявок
![Результат](здесь будет рисунок)

---

### 📌 Доля просроченных заявок
![Результат](здесь будет рисунок)

---

### 📌 Средняя длительность исполнения заявок
![Результат](здесь будет рисунок)

---

### 📌 Динамика количества заявок
![Результат](здесь будет рисунок)

---

### 📌 Распределение заявок по типам сбоев
![Результат](здесь будет рисунок)

---

### 📌 Анализ по регионам
![Результат](здесь будет рисунок)

---

## 📈 Результат

Сформирован дашборд, который:
- наглядно отображает ключевые показатели  
- позволяет анализировать динамику и структуру заявок  
- поддерживает масштабирование и добавление новых данных  

---

## 🛠 Задача 1. Обработка заявок 

В рамках задания необходимо:

1. Определить заявки, которые присутствуют на листе `WO`, и указать признак:
   - присутствует 
   - отсутствует 

2. Для заявок, присутствующих на листе `WO`, дополнительно подтянуть:
   - ID банкомата  
   - дату начала работ  
   - дату завершения работ  

Решение реализовано с использованием функции `ВПР (VLOOKUP)`.

---

## 📈 Результат

Сформирована итоговая таблица:
- с признаком наличия заявки  
- с дополненной информацией по банкоматам и датам  

![Результат](здесь будет рисунок)

---

## 🛠 Задача 2. Анализ заявок по РОО

В рамках задания выполнен анализ заявок по подразделениям (РОО):

1. Определён перечень уникальных РОО (столбец `C`), присутствующих на листе `WO`  

2. Рассчитано количество заявок по каждому РОО  

3. Определена средняя длительность заявок по всем РОО  
   *(разница между датой окончания и датой отправки)*  

4. Рассчитан общий фонд времени заявок по каждому РОО  

---

## 📈 Результат

Сформирована агрегированная таблица по РОО, включающая:
- количество заявок  
- среднюю длительность  
- суммарное время выполнения  

![Результат](здесь будет рисунок)

---

## 🛠 Задача 3. Анализ доступности и условное форматирование

В рамках задания выполнен анализ показателя доступности по регионам во времени:

- рассчитаны значения доступности по датам  
- применено условное форматирование для визуального анализа:  
  - значения **меньше 93%** выделены красным цветом  
  - значения **больше 96%** выделены зелёным цветом  

Форматирование реализовано с использованием формул в правилах условного форматирования.

---

## 📈 Результат

Сформирована таблица доступности по регионам с цветовой индикацией:
- зелёный — высокий уровень доступности  
- красный — критические значения  

![Результат](здесь будет рисунок)

---

## 🛠 Задача 4. Средневзвешенная доступность

В рамках задания необходимо рассчитать средневзвешенное значение доступности:

- показатель доступности (`столбец D`)  
- веса — количество устройств (`столбец C`)  

Расчёт выполнен по формуле средневзвешенного значения.

---

## 📈 Результат

Получено средневзвешенное значение доступности по всем филиалам:**95,2%**

![Результат](здесь будет рисунок)

---

## 🛠 Задача 5. Выявление аномальных отклонений

В рамках задания выполнен анализ количества ошибок по типам во времени с целью выявления аномальных отклонений.

Для каждого типа ошибки:
- определено среднее значение  
- проанализированы ежедневные отклонения  
- выявлены выбросы относительно нормального уровня  

Аномалии выделены с помощью условного форматирования.

Для выявления отклонений использовано правило условного форматирования на основе статистических показателей:

- верхняя граница:
  
  `значение > среднее + 1,5 × стандартное отклонение`

- нижняя граница:
  
  `значение < среднее − 1,5 × стандартное отклонение`

   **жёлтый цвет** — аномально высокие значения  
   **голубой цвет** — аномально низкие значения  

Коэффициент `1,5` выбран вместо `2`, так как при более строгом пороге выбросы не выявлялись, а текущее значение позволяет зафиксировать значимые отклонения.

---

## 📈 Результат

Сформирована таблица с визуально выделенными аномалиями по типам ошибок, что позволяет:
- быстро обнаружить нестабильные участки  
- выявить пики и просадки  
- провести дальнейший анализ причин  

![Результат](здесь будет рисунок)

---

## 🛠  Задача 6. Автоматизация с помощью макроса

В рамках задания необходимо реализовать автоматизацию расчётов с использованием VBA:

- добавить кнопку на лист  
- привязать к ней макрос  
- макрос должен выполнять расчёты, аналогичные задаче 2:
  - количество заявок по РОО  
  - средняя длительность  
  - общий фонд времени  

---

## 💻 Реализация

Макрос реализован на языке VBA и запускается по нажатию кнопки на листе.

```vba
    ActiveWindow.ScrollColumn = 2
    ActiveWindow.ScrollColumn = 3
    ActiveWindow.ScrollColumn = 5
    ActiveWindow.ScrollColumn = 6
    ActiveWindow.ScrollColumn = 7
    ActiveWindow.ScrollColumn = 8
    Columns("V:AB").Select
    ActiveWorkbook.PivotCaches.Create(SourceType:=xlDatabase, SourceData:= _
        "Таблица36", Version:=7).CreatePivotTable TableDestination:="6!R12C3:R17C6" _
        , TableName:="Сводная таблица10", DefaultVersion:=7
    Sheets("6").Select
    Cells(12, 3).Select
    With ActiveSheet.PivotTables("Сводная таблица10")
        .ColumnGrand = True
        .HasAutoFormat = True
        .DisplayErrorString = False
        .DisplayNullString = True
        .EnableDrilldown = True
        .ErrorString = ""
        .MergeLabels = False
        .NullString = ""
        .PageFieldOrder = 2
        .PageFieldWrapCount = 0
        .PreserveFormatting = True
        .RowGrand = True
        .SaveData = True
        .PrintTitles = False
        .RepeatItemsOnEachPrintedPage = True
        .TotalsAnnotation = False
        .CompactRowIndent = 1
        .InGridDropZones = False
        .DisplayFieldCaptions = True
        .DisplayMemberPropertyTooltips = False
        .DisplayContextTooltips = True
        .ShowDrillIndicators = True
        .PrintDrillIndicators = False
        .AllowMultipleFilters = False
        .SortUsingCustomLists = True
        .FieldListSortAscending = False
        .ShowValuesRow = False
        .CalculatedMembersInFilters = False
        .RowAxisLayout xlCompactRow
    End With
    With ActiveSheet.PivotTables("Сводная таблица10").PivotCache
        .RefreshOnFileOpen = False
        .MissingItemsLimit = xlMissingItemsDefault
    End With
    ActiveSheet.PivotTables("Сводная таблица10").RepeatAllLabels xlRepeatLabels
    With ActiveSheet.PivotTables("Сводная таблица10").PivotFields("РОО")
        .Orientation = xlRowField
        .Position = 1
    End With
    Range("C12").Select
    ActiveSheet.PivotTables("Сводная таблица10").CompactLayoutRowHeader = "РОО"
    Range("C13").Select
    ActiveSheet.PivotTables("Сводная таблица10").AddDataField ActiveSheet. _
        PivotTables("Сводная таблица10").PivotFields("Получатель"), _
        "Количество по полю Получатель", xlCount
    Range("D12").Select
    ActiveSheet.PivotTables("Сводная таблица10").PivotFields( _
        "Количество по полю Получатель").Caption = "Количество заявок"
    ActiveSheet.PivotTables("Сводная таблица10").AddDataField ActiveSheet. _
        PivotTables("Сводная таблица10").PivotFields("Длительность (часы)"), _
        "Количество по полю Длительность (часы)", xlCount
    Range("E12").Select
    With ActiveSheet.PivotTables("Сводная таблица10").PivotFields( _
        "Количество по полю Длительность (часы)")
        .Caption = "Средняя длительность в часах"
        .Function = xlAverage
    End With
    ActiveSheet.PivotTables("Сводная таблица10").AddDataField ActiveSheet. _
        PivotTables("Сводная таблица10").PivotFields("Длительность (часы)"), _
        "Количество по полю Длительность (часы)", xlCount
    Range("F12").Select
    With ActiveSheet.PivotTables("Сводная таблица10").PivotFields( _
        "Количество по полю Длительность (часы)")
        .Caption = "Общий фонд времени по заявкам в часах"
        .Function = xlSum
    End With
    With ActiveSheet.PivotTables("Сводная таблица10").PivotFields("РОО")
        .PivotItems("(blank)").Visible = False
    End With
    Range("I14").Select
End Sub
```
' Здесь будет код макроса
![Результат](здесь будет рисунок)

---

## 🛠  Задача 7. Анализ доступности по типу устройств и регионам

В рамках задания реализован интерактивный анализ доступности с возможностью выбора параметров:

- добавлен выбор типа устройства (УС)  
- реализована фильтрация данных по выбранному региону (РОО)  
- построены графики зависимости доступности:
  - по выбранному типу устройства  
  - по выбранному региону  
- реализована возможность сравнения:
  - сеть vs конкретный регион  

Для удобства анализа данные подготовлены и приведены к формату, подходящему для построения графиков.

---

## 📈 Результат

Построены визуализации, позволяющие:
- отслеживать динамику доступности во времени  
- сравнивать показатели по регионам и типам устройств  
- выявлять отклонения и тренды  

![Результат](здесь будет рисунок)

---
